# Set up git configuration. Use your own name and email address.
git config --global user.name "Robert Mollentze"
git config --global user.email "robmllze@gmail.com"

# Check the global configuration.
git config --global --list

# Set up VS Code as the default editor.
git config --global core.editor "code --wait"
git config --global core.editor